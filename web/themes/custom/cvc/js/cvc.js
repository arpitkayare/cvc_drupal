/**
 * @file
 * cvc behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.cvc = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
